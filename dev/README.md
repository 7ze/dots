# `DEV`

This is where I keep my `git` and `tmux` config for now

```custom
~
└── .config
    ├── git
    │   └── config           git config
    └── tmux
        └── tmux.conf        tmux config
```
