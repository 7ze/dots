# Fonts

```custom
~
├── .config
│   └── fontconfig
│       └── fonts.conf                      font configuration
└── .local
    └── share  
        └── fonts                           collection of fonts
```
